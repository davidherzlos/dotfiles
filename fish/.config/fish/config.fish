# Important: create on entry for each container you want to list in the herdr sidebar.
alias alenclaude='env HERDR_AGENT=claude docker exec -it alen-405-pgsql-webserver-1 bash -ic claude'
alias alenopencode='HERDR_AGENT=opencode docker exec -it alen-405-pgsql-webserver-1 bash -ic opencode'
